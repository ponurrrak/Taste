export const select = {
  templateOf: {

  },
  containerOf: {

  },
  all: {

  },

};

export const classNames = {

};

export const settings = {
  db: {
    url: '//' + window.location.hostname + (window.location.hostname=='localhost' ? ':3131' : ''),

  },

};

export const templates = {
  home: Handlebars.compile(document.querySelector().innerHTML),
};
